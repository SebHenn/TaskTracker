<Window x:Class="TaskTracker.Views.Windows.QuickAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTracker.Views.Windows"
        xmlns:controls="clr-namespace:TaskTracker.Controls"
        mc:Ignorable="d" Background="Transparent" WindowStyle="None"
        Title="QuickAddWindow" Height="150" Width="460"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border Background="{DynamicResource Brush.Window.Background}" CornerRadius="10" BorderBrush="{DynamicResource Brush.Accent}" BorderThickness="1">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Label Content="{DynamicResource QuickAddTitle}" Foreground="{DynamicResource Brush.Text.Primary}" FontWeight="SemiBold" />
            <controls:CustomTextBox x:Name="TitleBox" Grid.Row="1" Height="30" Margin="0,4"
                                    Hint="{DynamicResource QuickAddHint}" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Grid Grid.Row="2" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <ComboBox Height="26" HorizontalAlignment="Left" MinWidth="180"
                          ItemsSource="{Binding Projects}" DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedProject, Mode=TwoWay}" />
                <Button Grid.Column="1" Style="{DynamicResource WindowButton}" Padding="10,4" Margin="8,0,0,0"
                        Click="Confirm_Click" Content="{DynamicResource AddTask}" />
                <Button Grid.Column="2" Style="{DynamicResource WindowButton}" Padding="10,4" Margin="8,0,0,0"
                        Click="Cancel_Click" Content="{DynamicResource Cancel}" />
            </Grid>
        </Grid>
    </Border>
</Window>
