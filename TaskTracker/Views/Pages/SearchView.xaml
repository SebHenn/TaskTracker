<UserControl x:Class="TaskTracker.Views.Pages.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TaskTracker.Views.Pages"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <Label Content="{DynamicResource SearchResults}" FontSize="30" Foreground="{DynamicResource Brush.Text.Primary}" />
            <TextBlock FontSize="14" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="5,0,0,10">
                <Run Text="{Binding ResultCount, Mode=OneWay}" /> <Run Text="{DynamicResource ResultsFor}" /> "<Run Text="{Binding Query, Mode=OneWay}" />"
            </TextBlock>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Results}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="10" CornerRadius="3" Background="{DynamicResource Brush.Card.Background}" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick"
                                              Command="{Binding DataContext.ResultClickCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}" />
                            </Border.InputBindings>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Project.Name}" FontSize="12" Foreground="{DynamicResource Brush.Text.OnCard}" Opacity="0.8" />
                                    <TextBlock Text="  ▸  " FontSize="12" Foreground="{DynamicResource Brush.Text.OnCard}" Opacity="0.8" />
                                    <TextBlock Text="{Binding Task.Title}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.OnCard}" />
                                    <Border Style="{StaticResource TaskChip}" Margin="8,0,0,0"
                                            Visibility="{Binding Task.IsDone, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}">
                                        <TextBlock Style="{StaticResource TaskChipText}" Text="{DynamicResource Done}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding Task.Description}" FontSize="13" Foreground="{DynamicResource Brush.Text.OnCard}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
