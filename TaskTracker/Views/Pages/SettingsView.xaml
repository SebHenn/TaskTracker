<UserControl x:Class="TaskTracker.Views.Pages.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskTracker.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Label Foreground="White">u stoopid</Label>
        <ComboBox ItemsSource="{Binding AvailableLanguages}"
                  DisplayMemberPath="NativeName"
                  SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" />
        <CheckBox IsChecked="{Binding IsDark}" Command="{Binding ThemeCheckCommand}">isDark</CheckBox>
    </StackPanel>
</UserControl>
